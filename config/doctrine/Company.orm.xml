<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                   https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity
            name="App\Domain\Model\Company"
            table="company"
            repository-class="App\Domain\Repository\CompanyRepositoryInterface">

        <lifecycle-callbacks>
            <lifecycle-callback type="prePersist" method="initializeCreatedOn"/>
            <lifecycle-callback type="preUpdate" method="markAsUpdated"/>
        </lifecycle-callbacks>

        <!-- ID field from IdentifierTrait -->
        <id name="id" type="string" column="id">
            <generator strategy="NONE"/>
        </id>

        <field name="createdAt" column="created_at" type="datetime_immutable"/>
        <field name="updatedAt" column="updated_at" type="datetime_immutable" nullable="true"/>

        <field name="isActive" column="is_active" type="boolean"/>

        <field name="taxpayer" type="taxpayer_type" length="14">
            <options>
                <option name="fixed">true</option>
            </options>
        </field>

        <field name="fantasyName" column="fantasy_name" type="string" length="100" nullable="true"/>

        <!-- Relationships -->
        <one-to-many field="users" target-entity="App\Domain\Model\User" mapped-by="company" orphan-removal="false"/>

        <one-to-many field="ownedProjects" target-entity="App\Domain\Model\Project" mapped-by="owner" orphan-removal="false"/>

        <one-to-many field="boughtProjects" target-entity="App\Domain\Model\Project" mapped-by="buyer" orphan-removal="false"/>

    </entity>
</doctrine-mapping>